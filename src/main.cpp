#include "ofMain.h"
#include "ofApp.h"

//--------------------------------------------------------------
int main() {
    ofSetupOpenGL(SCREEN_WIDTH, SCREEN_HEIGHT, OF_WINDOW);
    ofRunApp(new ofApp());
}